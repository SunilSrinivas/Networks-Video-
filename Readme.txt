Steps to run the output:

PLease choose an S60 phone. It does not run on Android.

There is a jar files in jarfile_sms
just copy the jar file to a mobile device
open the jar file in the device. It installs the application.


The player video shows up.
Type the number in the destination.
THe camera takes snapshots and sends message the numbe rin the destination.


The same procedure is also done for the jarfile_mms jar file.

The code is present in the vsurveillance folder.
